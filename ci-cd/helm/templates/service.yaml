apiVersion: v1
kind: Service
metadata:
  name: {{ include "fairy-music.fullname" . }}
  labels:
    {{- include "fairy-music.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fairy-music.selectorLabels" . | nindent 4 }}
